<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.tesla</groupId>
    <artifactId>tesla</artifactId>
    <version>7</version>
  </parent>

  <groupId>io.tesla.maven.plugins</groupId>
  <artifactId>tesla-lifecycle</artifactId>
  <version>1.0.4-SNAPSHOT</version>
  <packaging>pom</packaging>

  <prerequisites>
    <maven>[3.1.0,)</maven>
  </prerequisites>

  <properties>
    <mavenVersion>3.1.2-SNAPSHOT</mavenVersion>
    <aetherVersion>0.9.0.M2</aetherVersion>
    <build.avoidance.version>0.0.6-SNAPSHOT</build.avoidance.version>
  </properties>

  <scm>
    <connection>scm:git:git@github.com/tesla/tesla-lifecycle-plugin.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/tesla/tesla-lifecycle-plugin.git</developerConnection>
    <url>http://github.com/tesla/tesla-lifecycle-plugin</url>
  </scm>

  <modules>
    <module>tesla-lifecycle-plugin</module>
    <module>tesla-lifecycle-plugin-its</module>
  </modules>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <source>1.6</source>
            <target>1.6</target>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.sonatype.plugins</groupId>
          <artifactId>sisu-maven-plugin</artifactId>
          <version>1.1</version>
          <executions>
            <execution>
              <id>generate-index</id>
              <goals>
                <goal>main-index</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.sonatype.plugins</groupId>
        <artifactId>nexus-staging-maven-plugin</artifactId>
        <extensions>true</extensions>
        <inherited>true</inherited>
        <configuration>
          <serverId>nexus</serverId>
          <nexusUrl>https://nexus.soma.salesforce.com/nexus/</nexusUrl>
          <stagingProfileId>7a74670f41f92</stagingProfileId>
        </configuration>
      </plugin>    
    </plugins>
  </build>
  
  <distributionManagement>
    <repository>
      <id>nexus</id>
      <url>https://nexus.soma.salesforce.com/nexus/service/local/staging/deploy/maven2/</url>
    </repository>
    <snapshotRepository>
      <id>nexus</id>
      <url>https://nexus.soma.salesforce.com/nexus/content/repositories/snapshots/</url>
    </snapshotRepository>
  </distributionManagement>
  
</project>
